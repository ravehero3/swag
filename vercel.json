{
  "version": 2,
  "public": true,
  "functions": {
    "server/src/prod.ts": {
      "maxDuration": 60
    }
  },
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/public"
      }
    },
    {
      "src": "server/src/prod.ts",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "server/src/prod.ts"
    },
    {
      "source": "/uploads/(.*)",
      "destination": "server/src/prod.ts"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
